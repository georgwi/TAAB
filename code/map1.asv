%function plant = map1
n = 100;
    plant = zeros(n,n);
	plant(1,:) = ones(1,n);
	plant(n,:) = ones(1,n);
	plant(:,1) = ones(1,n);
	plant(:,n) = ones(1,n);
    
    
    pos = round([n/4 n/3 n/2 2*n/3 3*n/4])
  


imagesc(plant)
%end